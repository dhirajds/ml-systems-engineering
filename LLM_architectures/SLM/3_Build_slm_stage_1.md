# Build an LLM
## Build an llm architecture
## collect data -> process data 
### Sample the data. -> What kind of data is used.
### ->tokenization.
### embedding used. ROPE vs any other.
## While building the architecture..think can you run that llm/ train that llm -> memory requirements.
### think like how many bytes for optimizer, how many bytes based on weights (float,int), number of weights.

## Attention mechanisim

# At the end of this , you will have the physical llm and data that is ready to be used to make it itelligent.

# Then comes pretraining.: on unlabelled dataset.
# If required -> do fine tuning.





